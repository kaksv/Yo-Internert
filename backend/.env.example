DATABASE_URL="postgresql://postgres:postgres@localhost:5432/hotspot?schema=public"
PORT=8787
JWT_SECRET="change-me-to-a-long-random-string-at-least-32-chars"
CORS_ORIGIN="http://localhost:5173"
# mock = instant paid session for UI demos | momo = wait for POST /api/webhooks/momo
PAYMENT_MODE="mock"
# Only for local integration testing when PAYMENT_MODE=momo
ENABLE_DEV_ROUTES="true"
