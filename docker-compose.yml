services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: hotspot
    ports:
      - "5432:5432"
    volumes:
      - hotspot_pg:/var/lib/postgresql/data

volumes:
  hotspot_pg:
