# Leave empty for local dev — Vite proxies /api to the backend (vite.config.ts).
# For production static build on Render, set to your API URL, e.g.:
# VITE_API_URL=https://hotspot-api.onrender.com
VITE_API_URL=
